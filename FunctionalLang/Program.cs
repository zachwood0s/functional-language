using FunctionalLang.Lexer;
using System;
using System.Linq;

namespace FunctionalLang
{
    class Program
    {
        public static void Main(string[] args)
        {
            /*
            string sample = @"f:A -> B";
            var tokenizer = new Tokenizer(TokenDefinitions.DefaultDefinitions);
            foreach(var token in tokenizer.Tokenize(sample))
            {
                Console.WriteLine($"Token: {token.TokenType} \t\tContents: {token.Value}");
            }
            Console.ReadKey();
            */
        }
    }
}
