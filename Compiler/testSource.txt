
sum: Int, Int -> Int
sum(a,b) = a + b

testFunction : Int -> Int
testFunction(a) = 
	let x = a * a;
		y = x * x
	in 
		let z = sum(x,y)
		in z

fib : Int -> Int
fib(x) =
	if x then 1
	else let
		fib1 = fib(x-1);
		fib2 = fib(x-2)
	in
		fib1 + fib2
